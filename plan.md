## План автоматизации тестирования веб‑сервиса покупки тура

**Автор:** Валентин Кнышов  
**Дата составления:** 20.04.2026  
**Наставник:** Андрей Пахомов

---

## 1. Перечень автоматизируемых сценариев с шагами и ожидаемыми результатами

### Позитивные сценарии

**Сценарий 1: Оплата тура дебетовой картой (успешный платёж)**

**Входные данные:**
* **Номер карты:** используется специальный тестовый номер APPROVED‑карты из ТЗ — `1111 2222 3333 4444`.
* **Срок действия:** генерируется автоматически в диапазоне «текущая дата + от 1 месяца до 6 лет» с помощью функции работы с датами. Формат — ММ/ГГ. Обоснование:
    * нижняя граница (от 1 месяца) исключает просроченные карты;
    * верхняя граница (до 6 лет) соответствует стандартной политике банков по максимальному сроку действия карт.
* **CVC:** генерируется случайное 3‑значное число в диапазоне 000–999. Обоснование — это соответствует стандарту большинства платёжных систем для CVC/CVV‑кодов.
* **Имя владельца:** генерируем автоматически (латиница, длина 2–50 символов, например, «JOHN SMITH»). Обоснование:
    * латиница — требование платёжных систем;
    * ограничение 2–50 символов — реалистичный диапазон для поля имени.

**Шаги:**
1. Открыть страницу оплаты тура.
2. Заполнить все поля валидными данными (сгенерированными автоматически, кроме номера карты).
3. Нажать кнопку «Продолжить».
4. Дождаться завершения операции.

**Ожидаемые результаты:**
* **UI:** сообщение «Успешно, Операция одобрена банком» (зелёного цвета), перенаправление на страницу подтверждения заказа.
* **СУБД:**
    * в таблице `order_entity` создана запись с `payment_id`;
    * в таблице `payment_entity` создана запись:
        * `transaction_id = payment_id` (из `order_entity`);
        * `status = 'APPROVED'`;
        * `amount` соответствует стоимости тура;
        * `created_at` — актуальная дата и время;
        * поля с данными карты — `NULL`.

---

**Сценарий 2: Оформление кредита на тур (успешное одобрение)**

**Входные данные:**
* **Номер карты:** используется тот же специальный тестовый номер APPROVED‑карты из ТЗ — `1111 2222 3333 4444`.
* **Срок действия:** генерируется автоматически в диапазоне «текущая дата + от 1 месяца до 6 лет», формат ММ/ГГ (например, при текущей дате 04/26 валидный срок может быть 06/30).
* **CVC:** случайное 3‑значное число (000–999).
* **Имя владельца:** генерируем автоматически (латиница, 2–50 символов).

**Шаги:**
1. Открыть форму оформления кредита.
2. Заполнить все поля валидными данными (сгенерированными автоматически, кроме номера карты).
3. Нажать кнопку «Продолжить».
4. Дождаться ответа от кредитного сервиса.

**Ожидаемые результаты:**
* **UI:** сообщение «Успешно, Операция одобрена банком» (зелёного цвета).
* **СУБД:**
    * в таблице `order_entity` создана запись с `credit_id`;
    * в таблице `credit_request_entity` создана запись:
        * `bank_id = credit_id` (из `order_entity`);
        * `status = 'APPROVED'`;
        * `loan_terms` содержит JSON с параметрами кредита;
        * `created_at` — актуальная дата;
        * поля карты — `NULL`.
      
### Негативные сценарии

#### Сценарий 3: Отказ в оплате дебетовой картой (DECLINED‑карта)

**Входные данные:**
* номер карты: `5555 6666 7777 8888` (DECLINED);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца: генерируем автоматически (латиница, 2–50 символов).

**Шаги:**
1. Открыть страницу оплаты.
2. Заполнить все поля валидными данными (кроме номера карты).
3. Нажать «Продолжить».
4. Дождаться ответа.

**Ожидаемые результаты:**
* **UI:** сообщение «Ошибка: платёж отклонён банком» (красного цвета), форма остаётся активной, перенаправления нет.
* **СУБД:** запись в `payment_entity` со статусом `DECLINED`, остальные поля (кроме `id` и `created_at`) — `NULL`, сумма тура указана.
---

#### Сценарий 4: Отказ в выдаче кредита (DECLINED‑карта)

**Входные данные:**
* номер карты: `5555 6666 7777 8888` (DECLINED);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца: генерируем автоматически (латиница, 2–50 символов).

**Шаги:**
1. Открыть форму кредита.
2. Заполнить все поля валидными данными (кроме номера карты).
3. Нажать кнопку «Продолжить».
4. Дождаться ответа сервиса.

**Ожидаемые результаты:**
* **UI:** сообщение «Банк отказал в выдаче кредита» (красного цвета), возможные причины (например, «Недостаточный доход»), форма доступна для повторной подачи.
* **СУБД:** запись в `credit_request_entity`:
    * `status = 'DECLINED'`;
    * `created_at` — корректная дата;
    * поля карты — `NULL`.

---

#### Сценарий 5: Неверный номер карты

**Входные данные:** 
* номер карты — генерируем случайный номер (например, `1234 5678 9012 3450`).
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца: генерируем автоматически (латиница, 2–50 символов).

**Шаги:**
1. Открыть любую форму оплаты.
2. Ввести неверный номер карты.
3. Заполнить остальные поля валидными данными.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** подсказка «Неверный номер карты» (красного цвета) под полем, кнопка неактивна, фокус на поле ошибки.
* **СУБД:** записей не создано.

---

#### Сценарий 6: Просроченная карта (прошлый месяц)

**Входные данные:**
* номер карты: `1111 2222 3333 4444` (APPROVED);
* срок действия: сгенерирован как «текущая дата – 1 месяц» (например, 03/2025 при текущей дате 04/2026);
* остальные поля — валидные (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты.
2. Заполнить все поля (с просроченным сроком).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Срок действия карты истёк» (красного цвета), поле срока подсвечено красным, отправка заблокирована.
* **СУБД:** состояние БД не изменилось.

---

#### Сценарий 7: Некорректный месяц в сроке действия (13‑й месяц)

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: месяц >12 (например, 13/28), год валидный;
* остальные поля — валидные (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты.
2. Заполнить все поля (с некорректным месяцем).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Месяц должен быть от 01 до 12» (красного цвета), поле подсвечено, кнопка неактивна.
* **СУБД:** изменений нет.

---

#### Сценарий 8: Срок действия более чем через 6 лет

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: сгенерирован как «текущая дата + 6 лет + 1 день» (например, 05/32 при текущей дате 04/26);
* остальные поля — валидные (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты.
2. Заполнить все поля (с длительным сроком).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Срок действия карты превышает допустимый период (6 лет)» (красного цвета), кнопка неактивна.
* **СУБД:** записей не создано.

---

#### Сценарий 9: Буквенные значения в сроке действия

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: случайная строка из букв (например, «ab/cd»);
* остальные поля — валидные (автоматически сгенерированные).

**Шаги:**
1. Открыть форму оплаты.
2. Заполнить все поля (с буквенным сроком).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Срок действия должен содержать только цифры в формате ММ/ГГ» (красного цвета), поле подсвечено, фокус возвращается к полю.
* **СУБД:** изменений нет.

---

#### Сценарий 10: Пустое поле срока действия

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: пустая строка;
* остальные поля — валидные (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты.
2. Заполнить все поля, кроме срока.
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Поле срока действия не может быть пустым» (красного цвета), фокус на поле срока, кнопка неактивна.
* **СУБД:** записей не создано.

---

#### Сценарий 11: Неверный CVC (2 цифры)

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: валидный (генерируется автоматически);
* CVC: 2 цифры (например, ввести «12»);
* имя владельца — валидное (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты или кредита.
2. Заполнить все поля (с коротким CVC).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «CVC должен содержать 3 цифры» (красного цвета), кнопка неактивна, фокус на поле CVC.
* **СУБД:** изменений в таблицах `payment_entity` и `credit_request_entity` нет.

---

#### Сценарий 12: Неверный CVC (буквы)

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: валидный (генерируется автоматически);
* CVC — случайная строка букв (например, «abc»). 
* Остальные данные — валидные (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты или кредита.
2. Заполнить все поля (с буквенным CVC).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «CVC должен содержать 3 цифры» (красного цвета), кнопка неактивна, фокус на поле CVC.
* **СУБД:** изменений в таблицах `payment_entity` и `credit_request_entity` нет.

---

#### Сценарий 13: Пустое поле CVC

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: валидный (генерируется автоматически);
* CVC — пустая строка
* Остальные данные — валидные (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты или кредита.
2. Заполнить все поля (с буквенным CVC).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «CVC должен содержать 3 цифры» (красного цвета), кнопка неактивна, фокус на поле CVC.
* **СУБД:** изменений в таблицах `payment_entity` и `credit_request_entity` нет.

---

#### Сценарий 14: Имя владельца на кириллице

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: валидный (генерируется автоматически);
* CVC — генерируется автоматически;
* имя — случайное значение на кириллице (например, ввести «ИВАН ИВАНОВ»).

**Шаги:**
1. Открыть форму оплаты/кредита.
2. Заполнить все поля (с кириллическим именем).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Имя владельца должно быть на латинице» (красного цвета), кнопка неактивна, фокус на поле имени.
* **СУБД:** изменений нет.

---

#### Сценарий 15: Пустое поле имени владельца

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: валидный (генерируется автоматически);
* CVC — (генерируется автоматически);
* имя — имя владельца — пустая строка.

**Шаги:**
1. Открыть форму оплаты.
2. Заполнить все поля, кроме имени.
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Поле имени владельца не может быть пустым» (красного цвета), фокус на поле имени, кнопка неактивна.
* **СУБД:** записей не создано.

---
#### Сценарий 16: Слишком длинное имя владельца (>50 символов)

**Входные данные:**
* номер карты: `1111 2222 3333 4444` (APPROVED);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца: сгенерирована строка длиной 51+ символ (например, «IVANIVANIVANIVANIVANIVANIVANIVANIVANIVANIVAN»).

**Шаги:**
1. Открыть форму оплаты/кредита.
2. Заполнить все поля (с длинным именем).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Имя слишком длинное (максимум 50 символов)» (красного цвета), кнопка неактивна, фокус на поле имени.
* **СУБД:** изменений нет.

---

#### Сценарий 17: Слишком короткое имя владельца (1 символ)

**Входные данные:**
* номер карты: `1111 2222 3333 4444` (APPROVED);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999); 
* имя владельца — 1 буква (например, «I»).


**Шаги:**
1. Открыть форму оплаты/кредита.
2. Заполнить все поля (с коротким именем).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Имя слишком короткое (минимум 2 символа)» (красного цвета), кнопка неактивна, фокус на поле имени.
* **СУБД:** изменений нет.

---

#### Сценарий 18: Имя владельца с недопустимыми символами (цифры и спецсимволы)

**Входные данные:**
* номер карты: `1111 2222 3333 4444` (APPROVED);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя — случайная строка с цифрами и спецсимволами (например, «IVAN123!@#»).

**Шаги:**
1. Открыть форму оплаты/кредита.
2. Заполнить все поля (с некорректным именем).
3. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Имя может содержать только буквы и пробелы» (красного цвета), кнопка неактивна, фокус на поле имени.
* **СУБД:** изменений нет.

---

#### Сценарий 19: Номер карты с пробелами в начале/конце

**Входные данные:**
* номер карты — ` 1111 2222 3333 4444 ` (пробелы в начале и конце);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца - генерируем.

**Шаги:**
1. Открыть форму оплаты.
2. Ввести номер карты с пробелами.
3. Заполнить остальные поля валидными данными.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** система автоматически обрезает пробелы, форма принимается (если остальные данные корректны).
* **СУБД:** в таблицах сохраняется номер без пробелов.

---

#### Сценарий 20: Номер карты со спецсимволами
**Входные данные:**
* номер карты — случайная строка со спецсимволами (например, `#@# $%^ &*()`);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца - генерируем.

**Шаги:**
1. Открыть форму оплаты/кредита.
2. Ввести некорректный номер карты.
3. Заполнить остальные поля валидными данными.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Неверный номер карты» (красного цвета), поле подсвечено красным, кнопка неактивна.
* **СУБД:** записей не создано.

---

#### Сценарий 21: Номер карты слишком короткий (например 12 цифр номера)

**Входные данные:**
* номер карты — 12 цифр (например, `1234 5678 9012`);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца - генерируем.

**Шаги:**
1. Открыть форму оплаты/кредита.
2. Ввести слишком короткий номер карты.
3. Заполнить остальные поля валидными данными.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Номер карты должен содержать 16 цифр» (красного цвета).
* **СУБД:** записей нет.

---

#### Сценарий 22: Номер карты слишком длинный (20 цифр)

**Входные данные:**
* номер карты — 20 цифр (например, `1234 5678 9012 3456 7890`);
* срок действия: генерируется автоматически в диапазоне «текущая дата + 1–6 лет»;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца - генерируем.

**Шаги:**
1. Открыть форму оплаты/кредита.
2. Ввести слишком короткий номер карты.
3. Заполнить остальные поля валидными данными.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Номер карты должен содержать 16 цифр» (красного цвета).
* **СУБД:** записей нет.

---

#### Сценарий 23: Срок действия с пробелами вокруг

**Входные данные:**
* номер карты: `1111 2222 3333 4444` (APPROVED);
* срок действия — ` 12/28 ` (пробелы до и после);
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца - генерируем.

**Шаги:**
1. Открыть форму.
2. Ввести срок с пробелами.
3. Заполнить остальные поля.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** система обрезает пробелы; если дата валидна — форма принимается.
* **СУБД:** сохраняется значение без пробелов.

---

#### Сценарий 24: Пустое поле номера карты

**Входные данные:**
* номер карты — пустая строка;
* срок действия генерируем автоматически;
* CVC: генерируется случайное 3‑значное число (000–999);
* имя владельца - генерируем.

**Шаги:**
1. Открыть форму оплаты/кредита.
2. Оставить поле номера карты пустым.
3. Заполнить остальные поля валидными данными.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Поле номера карты не может быть пустым» (красного цвета), фокус на поле номера, кнопка неактивна.
* **СУБД:** записей не создано.

---

#### Сценарий 25: Некорректный формат срока действия (только месяц или только год)

**Входные данные:**
* номер карты: `1111 2222 3333 4444`;
* срок действия: только месяц (например, «12») или только год (например, «28»);
* остальные поля — валидные (генерируется автоматически).

**Шаги:**
1. Открыть форму оплаты.
2. Ввести неполный срок действия.
3. Заполнить остальные поля валидными данными.
4. Нажать «Продолжить».

**Ожидаемые результаты:**
* **UI:** сообщение «Срок действия должен быть в формате ММ/ГГ» (красного цвета), поле подсвечено, кнопка неактивна.
* **СУБД:** изменений нет.
---

## 2. Перечень используемых инструментов с обоснованием выбора

| Инструмент          | Назначение                                | Обоснование выбора                                                                                                                                                     |
|---------------------|-------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Selenide**        | UI‑тестирование веб‑интерфейса            | Упрощает работу с Selenium WebDriver: встроенные ожидания, лаконичный синтаксис, автоматическая очистка ресурсов. Идеален для быстрого написания стабильных UI‑тестов. |
| **JUnit5**          | Фреймворк для написания и запуска тестов  | Современная версия JUnit с улучшенной поддержкой параметризованных тестов, расширений и аннотаций. Интегрируется с Selenide и Allure.                                  |
| **RestAssured**     | Проверка API‑запросов                     | Удобный DSL для тестирования REST‑сервисов на Java. Позволяет легко проверять статусы, тела ответов и заголовки.                                                       |
| **Allure**          | Генерация отчётов по результатам тестов   | Наглядные интерактивные отчёты с детализацией по шагам, скриншотами, логами и видео. Помогает быстро анализировать результаты.                                         |
| **Gradle**          | Сборка проекта и управление зависимостями | Популярный инструмент сборки для Java‑проектов. Обеспечивает гибкость конфигурации, интеграцию с CI/CD и управление зависимостями.                                     |
| **PostgreSQL/MySQL** | Тестовые базы данных                      | Соответствие требованиям задания. Возможность проверки работы приложения с разными СУБД.                                                                               |
| **Docker**          | Контейнеризация окружения                 | Изоляция тестового окружения, упрощение настройки и запуска СУБД и приложения. Позволяет воспроизвести production‑подобную среду.                                      |
| **Git/GitHub**      | Контроль версий и размещение репозитория  | Стандарт индустрии. Поддержка совместной работы, CI/CD‑интеграций и удобного обзора кода.                                                                              |

---

## 3. Перечень и описание возможных рисков при автоматизации

| Риск                                             | Описание                                                                                   | Меры по минимизации                                                                                                  |
|--------------------------------------------------|--------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------|
| **Недоступность симулятора банковских сервисов** | Сервис может быть временно недоступен или перегружен из‑за активности других студентов.    | Планировать тестирование в периоды низкой нагрузки. В тестах предусмотреть повторные попытки запросов (retry logic). |
| **Изменения в UI приложения**                    | Элементы интерфейса могут измениться, что приведёт к падению UI‑тестов.                    | Использовать паттерн Page Object Model для изоляции локаторов. Регулярно актуализировать локаторы.                   |
| **Проблемы с подключением к СУБД**               | Ошибки конфигурации или недоступность БД могут блокировать тесты, проверяющие записи в БД. | Автоматизировать проверку подключения к БД перед запуском тестов. Использовать Docker для развёртывания тестовых БД. |
| **Ложные срабатывания из‑за задержек**           | Задержки в ответах сервисов могут приводить к таймаутам.                                   | Увеличить таймауты в тестах. Добавить механизмы ожидания (explicit waits) для UI‑элементов в Selenide.               |
| **Ограничения по ресурсам**                      | Недостаток вычислительных ресурсов (память, CPU) при запуске контейнеров и тестов.         | Оптимизировать конфигурацию Docker. Запускать тесты последовательно, а не параллельно.                               |
| **Ошибки в тестовых данных**                     | Некорректные тестовые данные (карты, сроки) могут исказить результаты.                     | Использовать заранее проверенные тестовые данные. Валидировать входные данные перед тестом.                          |
| **Проблемы совместимости версий**                | Несовместимость версий инструментов (Selenide, JUnit, Gradle) может вызвать ошибки сборки. | Фиксировать версии зависимостей в `build.gradle`. Использовать Docker‑образы с предустановленным окружением.         |

---

## 4. Интервальная оценка с учётом рисков в часах

| Этап                                       | Минимальная оценка (ч) | Максимальная оценка (ч) | Комментарии                                                                                                |
|--------------------------------------------|------------------------|-------------------------|------------------------------------------------------------------------------------------------------------|
| **Планирование**                           | 8                      | 12                      | Составление плана, выбор инструментов, анализ рисков.                                                      |
| **Настройка окружения**                    | 16                     | 24                      | Установка ПО, настройка Docker, СУБД, подключение к сервисам.                                              |
| **Разработка UI‑тестов (Selenide)**        | 32                     | 48                      | Написание тестов для всех сценариев, включая позитивные и негативные.                                      |
| **Интеграция с API‑тестами (RestAssured)** | 12                     | 20                      | Проверка бизнес‑логики без привязки к UI.                                                                  |
| **Проверка работы с СУБД**                 | 8                      | 16                      | Написание SQL‑запросов для верификации записей в `order_entity`, `payment_entity`, `credit_request_entity`. |
| **Отладка и запуск тестов**                | 16                     | 24                      | Устранение ошибок, настройка отчётов (Allure), проверка стабильности.                                      |
| **Подготовка отчётных документов**         | 8                      | 12                      | Написание `Report.md`, `Summary.md`, оформление issue с багами.                                            |
| **Резерв на риски**                        | 16                     | 24                      | Учёт непредвиденных задержек, проблем с сервисами и т.д.                                                   |
| **Итого**                                  | **120**                | **180**                 | Общая оценка с учётом возможных задержек и доработок.                                                      |

## 5. План сдачи работ

**Общая трудоёмкость:** $120–180$ часов

| Этап | Время выполнения | Задачи этапа | Результаты этапа                                                                                                                                                                                                                                                                                                                                                                     |
|------|--------------|-------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Этап 1. Подготовка и планирование** | 8–12 часов | * анализ требований и уточнение сценариев; * составление списка тестовых данных; * выбор инструментов автоматизации; * оценка рисков и разработка мер по их минимизации; * создание структуры проекта | * документ `Plan.md` с полным описанием плана автоматизации;                                                                                                                                                                                                                                                                                                                         |
| **Этап 2. Настройка тестового окружения** | 16–24 часа | * установка и настройка инструментов (Selenide, JUnit 5, RestAssured, Allure, Gradle); * развёртывание тестовой среды с использованием Docker; * настройка подключения к тестовым базам данных (PostgreSQL/MySQL); * интеграция с CI/CD‑системой (при наличии) | * готовый Docker‑контейнер с настроенным окружением; * подключённые тестовые БД; * базовый конфигурационный файл `build.gradle`; * настроенные отчёты Allure                                                                                                                                                                                                                         |
| **Этап 3. Разработка UI‑тестов** | 32–48 часов | * написание UI‑тестов для позитивных сценариев (Сценарии 1–2); * разработка UI‑тестов для негативных сценариев (Сценарии 3–23); * реализация дополнительных сценариев (Сценарии 24–26); * внедрение паттерна Page Object Model для изоляции локаторов;| * код UI‑тестов в репозитории; * покрытие всех позитивных и негативных сценариев; * структурированный код с разделением на классы страниц; * базовые отчёты Allure после пробного запуска                                                                                                                                                                                            |
| **Этап 4. Разработка API‑тестов и интеграция с БД** | 12–20 часов | * написание API‑тестов с использованием RestAssured; * проверка бизнес‑логики без привязки к UI; * разработка SQL‑запросов для верификации записей в таблицах `order_entity`, `payment_entity`, `credit_request_entity`; * интеграция UI‑ и API‑тестов; * настройка проверок состояния БД после выполнения операций | * набор API‑тестов в репозитории; * SQL‑скрипты для проверки данных в БД; * интегрированные тесты, проверяющие UI, API и БД; * отчёты Allure с результатами первых полных прогонов                                                                                                                                                                                                   |
| **Этап 5. Запуск автотестов и анализ результатов** | 20–30 часов | * запуск полного набора автотестов на тестовом стенде; * сбор и анализ результатов выполнения тестов; * выявление и документирование дефектов; * отладка тестов, устранение ложных срабатываний; * повторный запуск проблемных сценариев | * отчёт Allure с результатами всех прогонов (проценты прохождения, упавшие тесты); * список обнаруженных дефектов в формате issue (с приоритетами); * обновлённый код тестов с исправленными ошибками; * скриншоты и логи для упавших тестов                                                                                                                                         |
| **Этап 6. Финальная проверка и подготовка отчётности** | 20–26 часов | * финальный запуск всех автотестов; * верификация исправления дефектов; * генерация полных отчётов Allure; * подготовка итогового отчёта по автоматизации; * оформление документации | * полные отчёты Allure (с шагами, скриншотами, логами); * итоговый документ `Report.md и Summary.md`|

---